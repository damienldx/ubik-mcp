/**
 * Compatibility facade for the modularized state manager.
 *
 * Existing imports still target `./state-manager`; implementation now lives in
 * `./state-manager/`.
 */

export * from "./state-manager/index";
